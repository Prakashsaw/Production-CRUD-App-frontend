import React from "react";

const EmailSentForResetPassword = () => {
  return <div>Email sent for reset password successfully. Check your email inbox(in spam folder aslo).</div>;
};

export default EmailSentForResetPassword;
